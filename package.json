{
  "name": "contractpulse-crm-monorepo",
  "private": true,
  "version": "1.0.0",
  "description": "ContractPulse — Enterprise CRM & B2B Contract Lifecycle Platform",
  "scripts": {
    "dev": "npm --prefix client run dev",
    "build": "npm --prefix client run build",
    "lint": "npm --prefix client run lint",
    "test": "npm --prefix client run test"
  }
}
